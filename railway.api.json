{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm turbo run build --filter=@coclaw/api..."
  },
  "deploy": {
    "startCommand": "pnpm --filter @coclaw/api exec prisma db push --accept-data-loss && pnpm --filter @coclaw/api start",
    "healthcheckPath": "/v1/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
