{
  "name": "@coclaw/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "scripts": {
    "compile": "prisma generate --schema prisma/schema.prisma && tsc -p tsconfig.json",
    "build": "cd ../.. && turbo run compile --filter=@coclaw/api^... && cd apps/api && prisma generate --schema prisma/schema.prisma && tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "prisma:generate": "prisma generate --schema prisma/schema.prisma",
    "prisma:push": "prisma db push --schema prisma/schema.prisma",
    "prisma:migrate:dev": "prisma migrate dev --schema prisma/schema.prisma",
    "clean": "rm -rf dist",
    "seed": "tsx src/scripts/seed.ts"
  },
  "dependencies": {
    "@coclaw/config": "workspace:*",
    "@coclaw/observability": "workspace:*",
    "@coclaw/shared-types": "workspace:*",
    "@fastify/cors": "^11.1.0",
    "@prisma/client": "^6.16.2",
    "fastify": "^5.6.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "prisma": "^6.16.2",
    "tsx": "^4.20.5",
    "vitest": "^3.2.4"
  }
}
